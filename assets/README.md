# Assets Directory

Place your icon.png file here (512x512 or larger recommended).

For now, the app will work without a custom icon and will use a system default.

To add a custom icon:
1. Create or obtain a 512x512 PNG image
2. Save it as `icon.png` in this directory
3. Rebuild the app
